---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: postgresconnections.postgres.silverswarm.io
spec:
  group: postgres.silverswarm.io
  names:
    kind: PostGresConnection
    listKind: PostGresConnectionList
    plural: postgresconnections
    singular: postgresconnection
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: PostGresConnection is the Schema for the postgresconnections
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PostGresConnection
            properties:
              clusterName:
                description: ClusterName references the CNPG cluster name
                type: string
              clusterNamespace:
                description: |-
                  ClusterNamespace is the namespace where the CNPG cluster is located
                  Defaults to the same namespace as the PostGresConnection if not specified
                type: string
              host:
                description: |-
                  Host is the PostgreSQL host (if not using CNPG service discovery)
                  Defaults to {clusterName}-rw service if not specified
                type: string
              port:
                default: 5432
                description: Port is the PostgreSQL port
                format: int32
                type: integer
              sslMode:
                default: require
                description: SSLMode specifies the SSL mode for the connection
                enum:
                - disable
                - allow
                - prefer
                - require
                - verify-ca
                - verify-full
                type: string
              superUserSecret:
                description: |-
                  SuperUserSecret references the secret containing superuser credentials
                  Defaults to {clusterName}-superuser if not specified
                properties:
                  name:
                    description: Name of the secret
                    type: string
                  namespace:
                    description: Namespace of the secret (defaults to same namespace
                      as PostGresConnection)
                    type: string
                required:
                - name
                type: object
              useAppSecret:
                default: false
                description: |-
                  UseAppSecret determines whether to use the app user secret instead of superuser
                  When true, uses {clusterName}-app secret for connection
                type: boolean
            required:
            - clusterName
            type: object
          status:
            description: status defines the observed state of PostGresConnection
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastChecked:
                description: LastChecked is the last time the connection was verified
                format: date-time
                type: string
              message:
                description: Message provides human readable status information
                type: string
              ready:
                description: Ready indicates if the connection is ready to be used
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
